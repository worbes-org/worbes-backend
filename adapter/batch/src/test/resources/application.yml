spring:
  profiles:
    active: batch_test
  datasource:
    batch:
      url: jdbc:postgresql://localhost:5432/batch_test
      username: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      driver-class-name: org.postgresql.Driver
