spring:
  profiles:
    active: batch_test
  datasource:
    batch:
      url: ${SPRING_BATCH_DATASOURCE_URL}
      username: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      driver-class-name: org.postgresql.Driver
