spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          use_sql_comments: true
          generate_statistics: true
  flyway:
    clean-disabled: false

logging:
  level:
    root: INFO
    com.worbes: DEBUG
